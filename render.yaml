services:
  - type: web
    name: wp-ai-agent
    env: python
    plan: free
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: ENV
        value: production
      - key: API_KEY
        sync: false